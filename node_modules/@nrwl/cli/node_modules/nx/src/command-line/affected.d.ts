import * as yargs from 'yargs';
import type { RawNxArgs } from '../utils/command-line-utils';
import { TargetDependencyConfig } from 'nx/src/config/workspace-json-project-json';
export declare function affected(command: 'apps' | 'libs' | 'graph' | 'print-affected' | 'affected', parsedArgs: yargs.Arguments & RawNxArgs, extraTargetDependencies?: Record<string, (TargetDependencyConfig | string)[]>): Promise<void>;
